spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8899
      #discovery:
      #  enabled: true
      #  service-id: config-server
eureka:
  server:
    enable-self-preservation: false